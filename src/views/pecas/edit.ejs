<div class="dashboard-header">
  <div>
    <h1>Editar Peça</h1>
    <p style="color: var(--on-surface-variant); font-size: 0.875rem;">Preencha as informações abaixo</p>
  </div>
  <div class="dashboard-actions">
    <a href="/pecas" class="btn btn-secondary"><i data-feather="arrow-left"></i> Voltar</a>
  </div>
</div>

<div class="card">
  <form action="/pecas/<%= peca.id %>?_method=PUT" method="POST">
    <div class="grid grid-cols-2 gap-4">
      <div class="form-group" style="grid-column: span 2;">
        <label for="nome">Nome *</label>
        <input type="text" id="nome" name="nome" class="form-control" value="<%= peca.nome %>" required>
      </div>
      <div class="form-group" style="grid-column: span 2;">
        <label for="descricao">Descrição</label>
        <input type="text" id="descricao" name="descricao" class="form-control" value="<%= peca.descricao %>">
      </div>
      <div class="form-group">
        <label for="valor_unitario">Valor Unitário *</label>
        <input type="number" step="0.01" id="valor_unitario" name="valor_unitario" class="form-control" value="<%= peca.valor_unitario %>" required>
      </div>
      <div class="form-group">
        <label for="estoque">Estoque *</label>
        <input type="number" id="estoque" name="estoque" class="form-control" value="<%= peca.estoque %>" required>
      </div>
    </div>
    <div class="mt-4"><button type="submit" class="btn">Atualizar</button></div>
  </form>
</div>
